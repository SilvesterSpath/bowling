import type { Match, Player, PlayerTitle } from '../types';

/** Title computation — implemented in Phase 5. */
export function computeTitles(_match: Match, _players: Player[]): PlayerTitle[] {
  return [];
}
